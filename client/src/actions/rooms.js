import { createAction } from 'redux-actions'

export const expandCard = createAction('expand card')
export const setOccupied = createAction('set occupied')
export const setRooms = createAction('set rooms')
